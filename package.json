{
  "name": "fin-cli",
  "version": "0.4.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --compile --outfile dist/fin",
    "clean": "bun unlink && rm -rfv dist",
    "db:clean": "rm -rf ~/.fin-cli"
  },
  "bin": {
    "fin": "./dist/fin"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cli-table3": "^0.6.5",
    "commander": "^14.0.2",
    "date-fns": "^4.1.0"
  }
}
